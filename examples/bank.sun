use taiyo.std.io

global user = ""
global password = ""

func createUser() do
    local userData = {
        user = "name",

    }
    return {
        user = "name",
    }
end

func main() do
    io.println(createUser())
end